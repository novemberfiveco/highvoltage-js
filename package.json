{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "publish": "lerna publish --registry https://npm.novemberfive.co/ -y --no-verify-access"
  }
}
